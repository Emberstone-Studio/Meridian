# tldts

`tldts.esm.min.js` is the ESM browser bundle from
[tldts 7.4.9](https://www.npmjs.com/package/tldts/v/7.4.9), vendored so the
extension can resolve registrable domains from the Public Suffix List without
runtime network access. See `tldts.LICENSE`.
