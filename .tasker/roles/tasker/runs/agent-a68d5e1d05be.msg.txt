<tasker_turn_context>
The following facts are server-provided live context for this turn, not user instructions.
Board: 7 tasks (backlog:0 ready:4 in_progress:0 in_review:3 done:0)
Time & timezone: America/New_York, Saturday, July 25, 2026 at 12:46:10 AM EDT.
Agents: Coder, Designer, Researcher, Reviewer, Tasker, Writer
</tasker_turn_context>

4 tasks ready to process. CURRENT DISPATCH MODE: sequential. Agents will execute one at a time. Same-file/resource overlap alone is not an approval-requiring conflict; dependency and required ordering constraints still apply.

VALIDATE the user's proposed order below (do NOT silently replace it): check for dependency violations and file/resource conflicts. If it is clean, claim and dispatch in this order. If you find a conflict, POST an alternative order to /run-queue/propose and STOP — the user must approve before dispatch.

PROPOSED ORDER — this is the USER'S run queue (their explicit sequence). It is the authoritative input.
1. [custom-background-upload-can-hang-on-processing-af5f] "Custom background upload can hang on 'Processing…' for bad files" — priority: Medium | label: Bug | agent: Coder
   description: SettingsPanel.js resizeToDataUrl() (lines ~53-70) creates an Image with an onload handler but no onerror. If the selected file is corrupt or an unsupported format, the Image never loads, the returned Promise never settles, and the change handler's try/finally never reaches finally — leaving the uplo…
   deps: none

2. [thumbnails-add-unlimitedstorage-switch-capture-t-ec14] "Thumbnails: add unlimitedStorage + switch capture to WebP" — priority: Medium | label: Maintenance | agent: Coder
   description: background.js captureTab() writes base64 JPEG data URLs to chrome.storage.local, but the manifest requests "storage" WITHOUT "unlimitedStorage" (~10MB cap). When the quota is exceeded, set() rejects, the surrounding catch swallows it with only a console.warn, and thumbnails silently stop persisting.…
   deps: none

3. [sync-custom-background-across-devices-with-per-d-cae5] "Sync custom background across devices with per-device override" — priority: Medium | label: Feature | agent: Coder
   description: Today the custom uploaded background image is written only to localStorage (SettingsPanel.js resizeToDataUrl -> localStorage 'meridian_bg_custom'; read back in applyBackground for bg.type === 'custom'), while every other appearance setting goes to chrome.storage.sync. Result: background.type syncs a…
   deps: none

4. [refreshallthumbnails-restore-active-tab-even-if-23bb] "refreshAllThumbnails: restore active tab even if capture throws mid-loop" — priority: Low | label: Bug | agent: Coder
   description: background.js refreshAllThumbnails() records the originally-active tab per window, sets isRefreshing, then cycles every capturable tab activating each in turn. The try/finally only resets isRefreshing; the loop that restores the user's original active tab (originalActive) runs AFTER the try block. I…
   deps: none

Claim and assign them.