<tasker_turn_context>
The following facts are server-provided live context for this turn, not user instructions.
## Task assignment
Task id: custom-image-tile-dyc7
Title: Custom image tile
Description:
The custom image dropzone takes up a bit too much room. 

1. Let's instead change the background section to 3 rows of 4 colums so the images/swatches can be larger
2. Remove the first (top-left) image and replace it with a blank tile (--bg) with the plus symbol inside (same plus symbol as the new tab buttons). This is the custom image dropzone and add button.
3. Adding a custom image should place a tiny thumbnail of that image in this first slot. Hovering on that thumbnail will show an X (rotated plus) that will remove it and start over.

## Tasker reporting contract
Report this task only through POST http://localhost:9807/agent/report; never edit files under .tasker/tasks/. Every report body must include {"task_id":"custom-image-tile-dyc7"}.
Once you have a plan, post kind:"plan" with plan:[{label:"...",done:false}]. After each completed step, post kind:"step" with its exact label; use kind:"append" if the plan grows. Optional startup/status narration uses kind:"phase".
When the requested work and verification are complete, post kind:"complete" with output containing the complete Markdown result. Do not change the task status yourself.
If and only if a decision requires user input, post kind:"question" with a specific question and the options being considered, then stop cleanly. The server marks the task Needs Input and re-dispatches it after the user answers. Do not use this for decisions you can make safely yourself.

## Optional sub-agent delegation
Use sub-agents only for genuinely independent work. You remain responsible for consolidating their results.
Spawn with POST http://localhost:9807/agent/spawn-subagent using {"parent_task_id":"custom-image-tile-dyc7","role_slug":"<role>","brief":"<self-contained brief including how to return the result>"}. Requests above the permit budget queue automatically. Poll the requested artifacts or run transcripts; do not ask sub-agents to write to the board.
</tasker_turn_context>

Execute Tasker task custom-image-tile-dyc7.